# Bug Bounty Helper - Pwdec
##### Installation

1 - Extract files in /var/www/html or any dir
2 - Start a apache server or python web server
in Linux :

service apache2 start
python -m http.server <PORT>
python -m SimpleHTTPServer <PORT>

That's it.

